#spring.application.name=project-task-service
server:
  port: 8083

spring:
  application:
    name: project-task-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/